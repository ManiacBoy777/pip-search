[build-system]
requires = [ "setuptools>=61.0", "wheel",]
build-backend = "setuptools.build_meta"

[project]
name = "pip-search-fix"
version = "1.0.0"
description = "Search PyPI packages from the command-line by scraping the web interface"
readme = "README.md"
classifiers = [ "Development Status :: 4 - Beta", "Intended Audience :: Developers", "License :: OSI Approved :: MIT License", "Operating System :: OS Independent", "Programming Language :: Python :: 3", "Programming Language :: Python :: 3.8", "Programming Language :: Python :: 3.9", "Programming Language :: Python :: 3.10", "Topic :: Software Development :: Libraries",]
keywords = [ "pip", "pypi", "search", "packages", "command-line",]
requires-python = ">=3.8"
dependencies = [ "requests>=2.25.0",]
[[project.authors]]
name = "@readwithai"
email = "talwrii@gmail.com"

[project.license]
text = "MIT"

[project.urls]
Homepage = "https://github.com/ManiacBoy777/pip-search"
Repository = "https://github.com/ManiacBoy777/pip-search"
Issues = "https://github.com/ManiacBoy777/pip-search/issues"

[project.scripts]
pip-search-fix = "pip_search_fix.main:main"
psf = "pip_search_fix.main:main"

[project.optional-dependencies]
dev = [ "black", "flake8", "pytest", "pytest-cov",]

[tool.setuptools]
packages = [ "pip_search_fix",]

[tool.black]
line-length = 88
target-version = [ "py38",]
include = "\\.pyi?$"

[tool.flake8]
max-line-length = 88
extend-ignore = [ "E203", "W503",]

[tool.setuptools.package-dir]
pip_search_fix = "pip_search_fix"

[tool.pytest.ini_options]
testpaths = [ "tests",]
python_files = [ "test_*.py",]
python_classes = [ "Test*",]
python_functions = [ "test_*",]
